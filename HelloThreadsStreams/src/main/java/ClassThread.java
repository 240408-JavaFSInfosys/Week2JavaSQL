
//This custom Thread extends Thread in order to Override the run() method
//But, we'll also implement Runnable to accomplish the same thing in our InterfaceThread
public class ClassThread extends Thread{

    @Override
    public void run(){
        //We could have defined a custom thread here...
        //But we'll do it via Runnable Interface instead (Check InterfaceThread)
    }

}
